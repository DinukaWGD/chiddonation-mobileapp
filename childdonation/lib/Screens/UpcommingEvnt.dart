import 'package:flutter/material.dart';

class UpcomminEvent extends StatelessWidget {
  const UpcomminEvent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text("Upcomming Event Details")),
    );
  }
}
